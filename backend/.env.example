PORT=5000
NODE_ENV=production

# PostgreSQL connection string for deployment (Railway, Supabase, Vercel, etc.)
DATABASE_URL=postgresql://user:password@host:port/dbname

# JWT configuration
JWT_SECRET=supersecretjwtkey_change_in_production
JWT_EXPIRES_IN=24h
